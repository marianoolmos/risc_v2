# RISC V2 (RISC-V  VHDL)

RISC V2 is a **32-bit RISC-V processor implemented in VHDL**, using a **classic 5-stage pipeline**:
                    
                            
                                 IF → ID → EX → MEM → WB
